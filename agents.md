# check Scoped Instructions

This directory inherits rules from the nearest parent `agents.md` in the directory tree, which is the repository root file at `../agents.md`.

## Local overrides
- Add directory-specific rules here when needed.
- These local rules may extend or override parent rules for files under `check/`.
