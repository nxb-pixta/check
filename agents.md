# Scoped Guidance: `check/`

Follow global rules from the repository root `AGENTS.md` first. If this directory is used as a standalone submodule checkout, apply these local rules and also follow any parent/superproject policy when present.

## Directory Purpose
- Lightweight check/smoke workspace for minimal validation artifacts.

## Directory-Specific Expectations
- Keep this directory minimal: prefer small, focused files for quick checks or notes.
- Avoid adding runtime/tooling dependencies here unless the owning team explicitly approves it.
- Do not duplicate global process rules in this file; add only local clarifications.
